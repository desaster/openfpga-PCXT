# dist

openFPGA packaging tree for the Analogue Pocket.

- `Cores/`: the core definition and its JSON metadata (`core.json`, `video.json`, ...)
- `Assets/`: default and bundled assets
- `Platforms/`: platform image and metadata
